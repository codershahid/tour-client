import React from "react";
import { createRoot } from "react-dom/client";

import "./index.css";

createRoot(document.getElementById("root")).render(
  <div className="text-center text-2xl uppercase">
    welcome to travelopia app
  </div>
);
